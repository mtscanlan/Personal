Document Access Test

Please verify you can access and edit this document by correcting the following sentence and saving the document.

Original: TEH quik browwn foxx JUmps ovar teh lazY dogg.

Corrected: The quick brown fox jumped over the lazy dog.


Document Instructions

This document will be used as a virtual whiteboard for sharing instructions, questions and responses. Some examples, may include writing code, design documentation or brainstorm lists. The interview will also be used in conjunction with the audio call.


Given a Node class with the following definition:

		2
	3	4	5
       6      7                       8

Output
-----
2
	3
		6
		7
	5
		8
	4


class Node{
	string Key;
	string ParentKey;
}

Implement the following method:

void PrintTree( List<Node> nodes ) {

Class RootNodes{
	string Key;
	List<RootNodes> children;
}

int main() {
	List<Node> nodes;
	rootNodes root = null
	foreach(node n in nodes) {
		if (n.Parent == null) {
			root = new Rootnodes { 
Key = n.Key,
children = new List<string>;
}
} else {
	dictionary[n.ParentKey].Add(n.Key);
}
}

findChildren(root, nodes);\
Console.WriteLine(root.Key);
printRootNodes(root, 0);
}

void findChildren (RootNodes root, List<Node> node) {
	foreach(Node n in Nodes) {
		if (n.ParentKey == root.Key) {
RootNode root = new RootNode() {
				key = n.key
				children = new list<RootNodes>;
}
			root.Children.Add(root );
			findChildren(root, node);
			}

}
}
}

void printRootNodes (RootNodes root, int level) {
	foreach(rootNode rn in root.Children) {
//Add tabs based on level
		Console.WriteLine(rn.Key);
		printRootNodes(rn, level+1);
}
}
